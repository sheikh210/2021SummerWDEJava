package basics;

public class LearnStatic2 {

    // Classes are comprised of 2 things: states & behaviors

    /*
     We are NOT able to call non-static class members (variables + methods) from other static methods (whether they're
     in the same class or an entirely different class)

     In order to call non-static members from static methods, we need to create an object of the class where the
     non-static member lives
     */

    public static void main(String[] args) {

    }
}
